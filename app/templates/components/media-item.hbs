<li>{{item.title}}</li>
{{#if item.finished}}
  <input onchange={{action "markFinished"}} type="checkbox"> {{#link-to (concat "add." route ".book") item.id}}info{{/link-to}}
{{else}}
  <input onchange={{action "markFinished"}} type="checkbox"> {{#link-to (concat "add." route ".book") item.id}}info{{/link-to}}
{{/if}}

